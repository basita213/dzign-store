{
  "name": "dzign-store",
  "version": "1.0.0",
  "private": true,
  "description": "DZIGN STORE — Custom Apparel Printing (Frontend + Vercel Serverless Backend)",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod",
    "db:setup": "node scripts/setup-db.js"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.10.0",
    "nodemailer": "^6.9.0",
    "uuid": "^10.0.0"
  },
  "devDependencies": {
    "vercel": "^37.0.0"
  }
}
